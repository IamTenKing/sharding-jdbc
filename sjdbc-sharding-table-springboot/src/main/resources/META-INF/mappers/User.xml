<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cxytiandi.sharding.repository.UserRepository">
    <!---->
    <!--<resultMap id="baseResultMap" type="com.cxytiandi.sharding.po.User">-->
        <!--<result column="id" property="id" jdbcType="INTEGER" />-->
        <!--<result column="city" property="city" jdbcType="VARCHAR" />-->
        <!--<result column="name" property="name" jdbcType="VARCHAR" />-->
        <!--<result column="create_time" property="createTime" jdbcType="DATETIME" />-->
    <!--</resultMap>-->
    
    <insert id="addUser" >
        INSERT INTO user (
           city, name,create_time
        )
        VALUES (
        #{city,jdbcType=VARCHAR},
        #{name,jdbcType=VARCHAR},
        #{createTime,jdbcType=TIMESTAMP}
        )
    </insert>
   
    <select id="list" resultType="com.cxytiandi.sharding.po.User">
        SELECT u.* FROM user u
    </select>
	
	<select id="findById" resultType="com.cxytiandi.sharding.po.User">
        SELECT u.* FROM user u WHERE u.id=#{id,jdbcType=INTEGER}
    </select>
    
    <select id="findByName" resultType="com.cxytiandi.sharding.po.User">
        SELECT u.* FROM user u WHERE u.name=#{name,jdbcType=VARCHAR}
    </select>


    <select id="listRange" resultType="com.cxytiandi.sharding.po.User">
        SELECT u.* FROM user u WHERE u.create_time between  #{start} and #{end}
     </select>
    
</mapper>
